language: node_js
sudo: false
cache:
  directories:
  - node_modules
env:
  - NODE_ENV=production
script:
  - npm run lint
  - npm test
before_deploy:
  - npm run compile
  - mkdir -p dist
  - cp index-prod.html dist/index.html
  - pip install --user awscli
  - export PATH=$PATH:$HOME/.local/bin
deploy:
  provider: s3
  region: us-east-1
  bucket: react-redux-boilerplate
  acl: public_read
  local_dir: dist
  skip_cleanup: true
  on:
    branch: master
after_deploy:
  - aws configure set preview.cloudfront true
  - aws cloudfront create-invalidation --distribution-id E1XDYVWECD7OV8 --paths /index.html /main.js /main.css
